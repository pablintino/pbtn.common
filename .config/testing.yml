profiles:
  local:
    molecule-timeout: 300
    molecules-timeouts:
        networking_setup: 1200
    default-driver: vagrant
    pinned-roles:
      podman:
        - ca_install
        - package_install
